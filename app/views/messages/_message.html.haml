.message
  .upper-message
    .message__name
      = message.user.name
    .message__date
      = message.updated_at
  .lower-message
    -if message.text.present?
      .message__text
        = message.text
    = image_tag message.image.url, class: 'message__image' if message.image.present?

